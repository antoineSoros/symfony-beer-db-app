{% extends 'base.html.twig' %}

{% block title %}Brewery{% endblock %}

{% block body %}
    <h1>Brewery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brewery.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brewery.name }}</td>
            </tr>
             <tr>
                <th>Country</th>
                <td>{{ brewery.country }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brewery_index') }}">back to list</a>

    <a href="{{ path('brewery_edit', {'id': brewery.id}) }}">edit</a>

    {{ include('brewery/_delete_form.html.twig') }}
{% endblock %}
